export default {
  plugins: {
    tailwindcss: {}, // Ensure the Tailwind CSS plugin is included
    autoprefixer: {}, // Other plugins like Autoprefixer if needed
    // Additional plugins if applicable
  },
}
